export enum SIZE {
  mini = 'mini',
  small = 'small',
  default = 'default',
  large = 'large',
}

export enum KIND {
  primary = 'primary',
  secondary = 'secondary',
  outline = 'outline',
  minimal = 'minimal',
  link = 'link',
}

export enum COLOR {
  primary = 'primary',
  accent = 'accent',
  warning = 'warning',
  negative = 'negative',
  positive = 'positive',
}
